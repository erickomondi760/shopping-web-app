const SectionTitle = ({ text }) => {
  return (
    <section className="border-b border-base-300 pb-5 mt-20">
      <h2 className="text-3xl font-medium tracking-wider capitalize">{text}</h2>
    </section>
  );
};
export default SectionTitle;
